export type TradeHistoryItem = {
  id: string;
  fromToken: string;
  toToken: string;
  fromAmount: number;
  toAmount: number;
  fromTokenImageUrl: string;
  toTokenImageUrl: string;
  fromValue: string;
  toValue: string;
  date: string;
};
